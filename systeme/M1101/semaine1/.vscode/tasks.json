{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile",
            "type": "shell",
            "command": "g++ -c hello.cpp -o hello.o && g++ hello.o -o hello 
            && ./hello",
            "group":  {
                "kind": "build",
                "isDefault": true
            }
        }        
    ]
}